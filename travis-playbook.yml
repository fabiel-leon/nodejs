---
- hosts: all
  remote_user: root
  become: yes

  vars:
    site_type: node
    domain: test.dev
  vars_files:
    - defaults/main.yml
    - vars/main.yml
  roles:
    - bbatsche.Nginx
  tasks:
    - include: tasks/main.yml
